const path = require('path');
const fs = require('fs');

module.exports = file => ***REMOVED***
    const dir = file == 'config' ? ['config.json'] : ['Preload_Data', `$***REMOVED***file***REMOVED***.json`];

    return JSON.parse(fs.readFileSync(path.join(__dirname, '..',  ...dir), 'utf8'));
***REMOVED***;